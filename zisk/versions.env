# ZISK_REF     ZisK tag the platform archive is built from and the zkvm_version
#              that names the artifacts
# RUST_NIGHTLY toolchain that builds it

ZISK_REPO=https://github.com/0xPolygonHermez/zisk
ZISK_REF=v1.1.0-alpha
RUST_NIGHTLY=nightly-2026-09-20
